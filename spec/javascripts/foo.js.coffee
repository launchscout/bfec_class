describe "Testing", ->
  it "is fun", ->
    expect(1 + 1).toBe(2)

describe "Another Context", ->
  it "has more tests", ->
    expect(true).toBeTruthy()
  it "has even more tests", ->
    expect(true).toBeTruthy()
